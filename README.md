# Task API

A simple CRUD API built with FastAPI.

The API allows users to create, read, update, and delete tasks stored in memory.

## Run

Install the dependencies:

```bash
pip install fastapi uvicorn